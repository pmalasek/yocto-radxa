From 065f58ce57d7f2417257d03d2df4e4e7931f4cc7 Mon Sep 17 00:00:00 2001
From: xPluto9 <xpluto9@example.com>
Date: Fri, 24 Apr 2026 10:00:00 +0000
Subject: [PATCH] arm64: dts: amlogic: meson-g12a-radxa-zero: add MCP2515 CAN

Enable an MCP2515 controller connected on SPICC1 CS0 with:
- 20MHz fixed clock
- IRQ on GPIOX_8 (falling edge)

Signed-off-by: xPluto9 <xpluto9@example.com>
---
 .../boot/dts/amlogic/meson-g12a-radxa-zero.dts     | 35 +++++++++++++++++++++
 1 file changed, 35 insertions(+)

diff --git a/arch/arm64/boot/dts/amlogic/meson-g12a-radxa-zero.dts b/arch/arm64/boot/dts/amlogic/meson-g12a-radxa-zero.dts
index 524f97afc0a6..c08e1f6e9f7e 100644
--- a/arch/arm64/boot/dts/amlogic/meson-g12a-radxa-zero.dts
+++ b/arch/arm64/boot/dts/amlogic/meson-g12a-radxa-zero.dts
@@ -7,6 +7,7 @@
 #include "meson-g12a.dtsi"
 #include <dt-bindings/gpio/meson-g12a-gpio.h>
 #include <dt-bindings/sound/meson-g12a-tohdmitx.h>
+#include <dt-bindings/interrupt-controller/irq.h>
 
 / {
 	compatible = "radxa,zero", "amlogic,g12a";
@@ -178,6 +179,13 @@ wifi32k: wifi32k {
 		clock-frequency = <32768>;
 		pwms = <&pwm_ef 0 30518 0>; /* PWM_E at 32.768KHz */
 	};
+
+	can0_osc: can0-osc {
+		compatible = "fixed-clock";
+		#clock-cells = <0>;
+		clock-frequency = <20000000>;
+		clock-output-names = "can0-osc";
+	};
 };
 
 &arb {
@@ -266,6 +274,17 @@ &ir {
 	pinctrl-names = "default";
 };
 
+&periphs_pinctrl {
+	mcp2515_int_pins: mcp2515-int-pins {
+		mux {
+			groups = "GPIOX_8";
+			function = "gpio_periphs";
+			bias-pull-up;
+			drive-strength-microamp = <4000>;
+		};
+	};
+};
+
 &pwm_AO_cd {
 	pinctrl-0 = <&pwm_ao_d_e_pins>;
 	pinctrl-names = "default";
@@ -371,6 +390,22 @@ &sd_emmc_c {
 	vqmmc-supply = <&vcc_1v8>;
 };
 
+&spicc1 {
+	status = "okay";
+	pinctrl-names = "default";
+	pinctrl-0 = <&spicc1_pins>, <&spicc1_ss0_pins>;
+	#address-cells = <1>;
+	#size-cells = <0>;
+
+	can0: can@0 {
+		compatible = "microchip,mcp2515";
+		reg = <0>;
+		spi-max-frequency = <10000000>;
+		interrupt-parent = <&gpio_intc>;
+		interrupts = <GPIOX_8 IRQ_TYPE_EDGE_FALLING>;
+		clocks = <&can0_osc>;
+	};
+};
+
 &tdmif_b {
 	status = "okay";
 };
-- 
2.34.1
